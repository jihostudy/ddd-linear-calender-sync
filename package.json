{
  "name": "linear-gcal-sync",
  "version": "1.0.0",
  "private": true,
  "description": "Linear issue due dates → Google Calendar events (one-way sync)",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.10.0",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "googleapis": "^144.0.0"
  }
}
